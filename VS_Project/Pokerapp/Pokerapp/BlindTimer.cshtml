@using WebMatrix.Data;
@{
    string connectionString = @"Data Source=(LocalDB)\MSSQLLocalDB;AttachDbFilename=|DataDirectory|\Database.mdf;Integrated Security=True";
    string provider = "System.Data.SqlClient";
    Database db = Database.OpenConnectionString(connectionString, provider);

    //get the url
    string url = Request.Url.AbsoluteUri;
    string roomCode = Request.QueryString["room"];
    string table = Request.QueryString["table"];
    string blind = Request.QueryString["blind"];
    int index = Request.QueryString["blindIndex"].AsInt();

    dynamic blinds = db.Query("SELECT ValueSB, ValueBB FROM Blinds WHERE Room_Code = @0 AND ID = @1", roomCode, index);
    db.Execute("UPDATE Room SET Blind_Round = @0 WHERE Room_Code = @1", index + 2, roomCode);

    Response.Write(index + "-" + blinds.ValueSB + "-" + blinds.ValueBB);
}