@using WebMatrix.Data;
@{
    //Database connectie
    string connectionString = @"Server = tcp: pokerapptres.database.windows.net;Database=Pokerapp;User ID =dannypas00@pokerapptres.database.windows.net;Password=Lugia123.;Trusted_Connection=False;Encrypt=True";
    string provider = "System.Data.SqlClient";
    Database db = Database.OpenConnectionString(connectionString, provider);

    //get the url
    string url = Request.Url.AbsoluteUri;
    string urlcode = url.Substring(url.Length - 7);

    //Get the round_time
    var selectTimer = db.QuerySingle("SELECT Round_Time FROM Room WHERE Room_Code = @0", Request["roomcode"].AsInt(), Request["table"].AsInt());
    Response.Write(selectTimer.Round_Time);

}