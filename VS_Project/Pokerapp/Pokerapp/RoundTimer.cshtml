@using WebMatrix.Data;
@{ 
    //get the url
    string roomCode = Request.QueryString["roomCode"];
    int timer = Request.QueryString["totalSec"].AsInt();
    int roundTimer = Request.QueryString["roundTimer"].AsInt();

    int round = timer / roundTimer;
    Response.Write(round);
}